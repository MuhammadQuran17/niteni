# GitLab NPM Registry Configuration
# Replace ${CI_PROJECT_ID} with your actual GitLab project ID for local development
# In CI, these variables are automatically available

//gitlab.com/api/v4/projects/${CI_PROJECT_ID}/packages/npm/:_authToken=${CI_JOB_TOKEN}
